POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=finance_agent

# Any random string; used to sign auth JWTs. Generate with: openssl rand -base64 48
JWT_SECRET=

# Google Cloud Console -> APIs & Services -> Credentials -> OAuth client ID (Web application)
# Required for "Login with Google". Leave empty to disable it.
GOOGLE_CLIENT_ID=
